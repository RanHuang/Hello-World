1. #!(shebang) 解析器名称及其路径

2. $bash     #启动bash解析器 
	$echo $SHELL

3. 位操作符
	<< -- 左移
	>> -- 右移
	&  -- 与
	|  -- 或
	~  -- 非
	!  -- 取反
	^  -- 异或

4. 逻辑操作符
	&&
	||

5. read
	-d -- 字符表示输出终止(非换行符)
	-n -- 最大字节数
	-p -- 提示信息
	-s -- 静默输入，不回显
	-t -- 超时等待
	-u -- 指定文件描述符读取

6.
